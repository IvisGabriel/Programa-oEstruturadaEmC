#include <stdio.h>

int main() {
    int numero, produtório = 1;

    printf("Digite os números inteiros positivos (digite 0 para encerrar):\n");

    // Laço para ler os números até que seja digitado 0
    while (1) {
        // Leitura do número
        scanf("%d", &numero);

        // Condição de parada: se o número for 0, o programa termina
        if (numero == 0) {
            break;
        }

        // Verifica se o número é positivo e par
        if (numero >
