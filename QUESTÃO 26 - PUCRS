#include <stdio.h>

int main() {
    // Laço que percorre os números de 1000 a 1999
    for (int i = 1000; i <= 1999; i++) {
        // Verifica se o número dividido por 11 tem resto 5
        if (i % 11 == 5) {
            printf("%d\n", i);  // Imprime o número
        }
    }

    return 0;
}
