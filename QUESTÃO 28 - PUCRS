#include <stdio.h>

int main() {
    int n;
    float S = 0.0;

    // Leitura do valor de n
    printf("Digite um valor inteiro e positivo para n: ");
    scanf("%d", &n);

    // Verificando se n é positivo
    if (n <= 0) {
        printf("O valor de n deve ser um número positivo!\n");
        return 1; // Encerra o programa em caso de valor inválido de n
    }

    // Calculando a soma e escrevendo cada termo
    for (int i = 1; i <= n;
