#include <stdio.h>

int main() {
    int N;

    // Leitura dos 20 valores e cálculo das tabuadas
    for (int i = 1; i <= 20; i++) {
        printf("Digite o %dº valor (N): ", i);
        scanf("%d", &N);

        // Exibe a tabuada de 1 até N
        printf("Tabuada de %d:\n", N);
        for (int j = 1; j <=
